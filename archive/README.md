# Archived projects

Finished or parked research projects live here, grouped by domain under
`archive/<domain>/`. A parked project keeps its full folder including sources,
notes, findings, and outputs so it can be resumed later.

Status in the project `README.md` should be set to `archived` when moved here.